; ### Running `check-sat`

(run-schedule
    (saturate (run width))
    ; Run once ruleset
    (run once)
    ; Alternate between saturating safe ruleset and running some unsafe rules
    (repeat 2
        (saturate (run safe :until (= true (ProvenUnsat))) (run width))
        (run unsafe :until (= true (ProvenUnsat)))))
